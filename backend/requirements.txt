Flask==3.0.3
APScheduler==3.10.4
python-dotenv==1.0.1
requests==2.32.3
Werkzeug==3.0.3

# Optional - only needed if you enable OCR / SMS / Voice call features.
# Install if you want real functionality for these; the app runs fine
# without them (it falls back to demo data / "not configured" messages).
pytesseract==0.3.13
Pillow==10.4.0
PyMuPDF==1.24.9   # lets OCR read PDF prescriptions (optional)
twilio==9.3.2

gunicorn
